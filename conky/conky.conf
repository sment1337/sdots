-- Conky, a system monitor https://github.com/brndnmtthws/conky
--
-- This configuration file is Lua code. You can write code in here, and it will
-- execute when Conky loads. You can use it to generate your own advanced
-- configurations.
--
-- Try this (remove the `--`):
--
--   print("Loading Conky config")
--
-- For more on Lua, see:
-- https://www.lua.org/pil/contents.html
--
-- Conky Lua API: https://conky.cc/lua

-- Configuration settings: https://conky.cc/config_settings
conky.config = {
    alignment = 'bottom_left',
    background = false,
    border_width = 1,
    cpu_avg_samples = 2,
    default_color = 'white',
    default_outline_color = 'white',
    default_shade_color = 'white',
    double_buffer = true,
    draw_borders = false,
    draw_graph_borders = true,
    draw_outline = false,
    draw_shades = false,
    extra_newline = false,
    font = 'DejaVu Sans Mono:size=9',
    gap_x = 60,
    gap_y = 60,
    minimum_height = 5,
    minimum_width = 5,
    net_avg_samples = 2,
    no_buffers = true,
    out_to_console = false,
    out_to_ncurses = false,
    out_to_stderr = false,
    out_to_wayland = false,
    out_to_x = true,
    own_window_transparent = true,
    own_window_argb_visual = true,
    own_window = true,
    own_window_class = 'Conky',
    own_window_type = 'override',
    own_window_hints = 'undecorated,sticky,below,skip_taskbar,skip_pager',
    show_graph_range = false,
    show_graph_scale = false,
    stippled_borders = 0,
    update_interval = 0.5,
    uppercase = false,
    use_spacer = 'none',
    use_xft = true,
}

-- Variables: https://conky.cc/variables
conky.text = [[
${font DejaVu Sans Mono:size=24}${alignc 12}${time %H:%M}
${font DejaVu Sans Mono:size=9}${time %A %d %B %Y}
${color red}Info:$color ${scroll 32 Conky $conky_version - $sysname $nodename $kernel $machine}
${color #ffcb48}General ${hr 1}
${color grey}Uptime:$color $uptime
${color grey}Swap Usage:$color $swap/$swapmax - $swapperc% ${swapbar 10}
${color grey}File systems: $color${fs_used /}/${fs_size /} ${fs_bar 10 /}

${color #ffcb48}Processes ${hr 1}
${color red}RAM Usage:$color $memperc% ${membar 10, 70} $mem/$memmax
${color red}CPU Usage:$color $cpu% ${cpubar 10}
${color red}Name                   PID    CPU%   MEM%
${color lightgrey}${top name 1}    ${top pid 1} ${top cpu 1} ${top mem 1}
${color lightgrey}${top name 2}    ${top pid 2} ${top cpu 2} ${top mem 2}
${color lightgrey}${top name 3}    ${top pid 3} ${top cpu 3} ${top mem 3}
${color lightgrey}${top name 4}    ${top pid 4} ${top cpu 4} ${top mem 4}
${cpugraph 30, gradient colour 2 -t -l}
${color grey}Frequency:$color $freq MHz
${color grey}Processes:$color $processes  ${color grey}Running:$color $running_processes

${color #ffcb48}Wi-Fi ${hr 1}
${color lightgrey}Wireless signal: $color${wireless_link_qual enp2s0f0}%
${color lightgrey}IP address: $color${addr enp2s0f0}
${color lightgrey}Download speed: $color${downspeed enp2s0f0}/sec
${downspeedgraph enp2s0f0}
${color red}Downloaded: $color${totaldown enp2s0f0} $alignr ${color green}Uploaded: $color${totalup enp2s0f0}

${color #ffcb48}Battery ${hr 1}
${color red}Battery 0:$color ${battery_percent BAT0}% ${battery_bar 10 BAT0} 
     Draw: ${battery_power_draw BAT0}W 
Time left: ${battery_time BAT0}
]]
